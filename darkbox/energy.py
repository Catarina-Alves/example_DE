import numpy as np

def abs_area(x):
	"""Calculate the absolute value of x.

	Some more detail about absolute value
	
	Parameters
	----------
	x : ndarray
		The signal for which to compute the absolute value

	Returns
	-------
	
	"""	
	return np.sum(np.abs(x))
